# Fallen rainbow — 改进空间与建议

> 日期：2026-08-29 ｜ 当前交付 `fallen_rainbow.zip` **13057 B**（上限 13312 B，余量 **255 B**）

---

## 1. 六维评分现状（对照 js13k 评价标准）

| 维度 | 估分 | 现状 |
| --- | --- | --- |
| 主题 | 3.5 / 5 | 「死亡与轮回 + 颜色=记忆」隐喻完整，有王冠抉择/真结局分支；但呈现方式单一（纯文本覆盖层） |
| 玩法 | 3.5 / 5 | 机关+脚本驱动扎实（浮板/按钮/蹦床/黑洞/单向）；缺收集/成长闭环与检查点 |
| 音频 | 3 / 5 | 零散音符环境乐 + 带冠诡异变奏 + 缓入缓出 + 按钮静音；仍无混音/音量分层/死亡 BGM 淡出 |
| 创新 | 4 / 5 | 隐藏关、倒置重力、王冠元叙事、程序化音频，相对最强 |
| 美术 | 3 / 5 | 双色像素是特色；但每章仅换配色、画面静止，缺层次/氛围元素（无相机固定屏，视差不可行） |
| 控制 | 3.5 / 5 | 键盘 + 触屏虚拟按键 + 横屏适配；缺手感优化（土狼时间/跳跃缓冲）与手柄 |

## 2. 已完成优化（截至 2026-08-29）

- **程序化 BGM**：零散音符环境乐 + 带冠诡异变奏（`sound.js`，单定时器 setTimeout 自排，缓入缓出）
- **静音按钮**：左上 `♪` 常驻可点（取代 M 键），触屏/鼠标均可用，静音变红
- **N 键跳关**：DEBUG 标记（`--define DEBUG=false` 构建剔除，正式产物零成本）
- **触屏虚拟按键**：左下 `◀ ▶` 移动、右下 `▲` 跳跃（`@media (pointer: coarse)` 才显示）
- **横屏适配**：32×16 宽画布等比缩放 + 每帧视口检查（覆盖嵌入/小窗）
- **腾空间**：删黑洞尾迹粒子（-182B）+ copy.js 文案精简（-47B）
- **视差背景**：尝试后因无相机、前景不移动而移除（教训：固定屏不做按玩家平移的视差）

## 2.1 已完成优化（2026-08-30 追加）

- **主题强化-D1 王冠光晕**：戴冠时王冠周围一圈随色相脉动的辉光描边（`entities.js` `player_renderCrown`，`strokeRect` + `globalAlpha` 脉动）
- **主题强化-D2 死亡 glitch**：死亡瞬间世界画面切成 5 条横向条带做水平错位闪烁 0.35s（`game_core.js` `gameRender` + `GAME_deathAt`，随 `GAME_awaitingRespawn` 生效并渐弱）
- **死亡 BGM 淡出**：`bgmStop(0.6)` 短时音量渐降到 0 而非骤停（`sound.js`：`playSound` 返回增益节点、`bgmTick` 记录 `bgmG`、`bgmStop(f)` 用 `cancelScheduledValues`+`linearRampToValueAtTime`）
- 构建后 zip 13057→13202（+145B），余量 110B，PASS ≤13312

## 3. 零/低成本改进（≤255 B，可直接做）

### 3.1 土狼时间 + 跳跃缓冲（控制）—— 约 60 B，最高性价比

- 现状：`entities.js` 中 `player_getDY` 仅在 `player_isOnGround()` 的**当帧**判定跳跃
- 建议：离开平台边缘后 0.1s 内仍可跳（coyote time）；起跳前 0.1s 按跳有效（jump buffer）
- 收益：平台跳跃手感从「能用」到「跟手」，是控制维度最大单项提升

### 3.2 ~~死亡时 BGM 淡出而非骤停~~ ✅ 已完成（2026-08-30）

- 现状：`game_core.js` `gameKillPlayer` 直接 `bgmStop()`
- 建议：改为短时间音量渐降到 0（复用 `playSound` 的 ADSR 或加一个增益衰减）

### 3.3 ~~触屏端静音~~ ✅ 已完成

- 改为左上 `♪` 常驻可点按钮（触屏/鼠标均可用，静音变红），M 键已删除

## 4. 需先腾体积的改进（200–500 B/项）

### 4.1 色彩碎片收集（玩法 + 主题，收益最大）

- 呼应「颜色=记忆」：收集碎片逐步恢复画面色彩（拿冠后逐章褪色 `desaturateColor` 已实现，缺「主动收集恢复」的正向闭环）
- 需要：新 trap/实体 + 收集计数 + 关卡放置 + 回色动画，约 400–500 B

### 4.2 美术层次元素（详见下方「美术优化方案」）

- 无相机固定屏，按玩家平移的视差已证实不可行（曾实现后移除）；改为静态剪影/背景色带/自漂元素

### 4.3 手柄支持（控制）

- Gamepad API 映射到 `actions`，约 200 B

### 4.4 长关卡检查点（玩法）

- 当前每关只有一个出生点，后期关卡重开成本高

## 5. 美术优化方案（2026-08-29 新增）

> 硬约束：**固定屏幕、无相机、前景不移动** → 禁做按玩家位置平移的视差（已实现后移除）。
> 可做的分两类：① 静态装饰（不移动）；② 自身运动元素（不依赖玩家位置）。

### A. 静态装饰（安全优先，不移动）—— 合计约 100–160 B

- **A1 每章静态远景剪影**（~60–100 B）：天空区用前景色低透明度画固定剪影（山峦/城市/森林/星海，贴合章节配色主题），仅作背景图案、不随玩家移动。物理上成立（区别于废弃的视差）。
- **A2 背景色带/天际线渐变**（~40–60 B）：单色背景顶部加 2–3 条横向色带（同色系明度微变），模拟天空渐层/景深。
- 收益：画面立刻有层次感，零手感风险，最适合首批落地。

### B. 自身运动元素（给固定画面"呼吸感"）—— 约 100–190 B

- **B1 自漂云**（~60–80 B）：时间驱动的云团缓慢横移（不依赖玩家位置）；此前视差实现里"云随时间漂移"部分是成立的，可单独保留。
- **B2 闪烁星星**（~40–60 B）：暗背景章节顶部随机相位闪烁白点。
- **B3 水流/岩浆明暗脉动**（~60–100 B）：装饰区块做正弦明暗脉动。

### C. 立体感与角色细节 —— 约 140–250 B

- **C1 平台高光/阴影**（~60–100 B）：实体块顶边 1px 高光 + 底边 1px 阴影（bg/fg 变体），伪 3D 体积感。
- **C2 角色 squash & stretch**（~80–150 B）：跳跃拉长、落地压扁 + 落地尘土粒子。

### D. 主题强化（呼应「颜色=记忆」）—— 约 120–240 B

- **D1 王冠光晕/环绕**（~60–120 B）：戴冠时王冠周围粒子/光晕闪烁。
- **D2 过关彩虹扫屏 / 死亡 glitch 增强**（~60–120 B）：转场视觉冲击。

### E. UI 美术微调（~20–40 B）

- 死亡计数、关卡标题描边/字重微调。

> 建议顺序：A（静态剪影+色带）→ B（云/星）→ C（高光/挤压）→ D/E。
> A+B 合计约 160–260 B，在现余量 255 B 内可落地 1–2 项；其余需再腾空间（文案/CSS）。

## 6. 腾体积杠杆（memory 记录）

- **文案** `copy.js`（约 1200 B 唯一内容块，压缩效率最差）—— 精简台词可一次性腾出几十 B（死亡原因/重复句已精简过一轮）
- **CSS 冗余** 精简（`index.html`）
- 若需大功能：可权衡精简 BGM（当前已是零散音符最省形态，余地不大）或触屏代码

## 7. 建议优先级

1. **土狼时间 + 跳跃缓冲**（~60 B，手感质变，零风险）
2. **死亡 BGM 淡出**（~40 B，体验细腻）
3. **美术：静态远景剪影 + 背景色带**（§5-A，~100–160 B，画面层次感，零手感风险）
4. **美术：自漂云 / 闪烁星**（§5-B）
5. **色彩碎片收集**（做之前先靠精简文案腾空间）
6. 手柄 / 检查点 / 角色 squash（按需）

> 注意：任何改动需 `npm run build`（或 `build\build_all.bat`）后确认 `fallen_rainbow.zip ≤ 13312`，并推送 git。
